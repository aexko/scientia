describe('sample_spec.cy.js', () => {
  it('playground', () => {
    expect(true).to.equal(false)
    // cy.mount()

    // 1st phase : ARRANGE - setup initial app state
    // 2nd phase : ACT - take an action
    // 3rd phase : 
  })
})